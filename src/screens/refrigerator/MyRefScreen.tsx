import styled from 'styled-components/native';
import { Text } from 'react-native';

const MyRefScreen = () => {
 return (
 <MyRefScreenBlock>
  <Text>술장고 메인</Text>
 </MyRefScreenBlock>
 );
};

const MyRefScreenBlock = styled.View``;

export default MyRefScreen;
