import styled from 'styled-components/native';

const SignupScreen = () => {
 return (
 <SignupScreenBlock>
 
 </SignupScreenBlock>
 );
};

const SignupScreenBlock = styled.View``;

export default SignupScreen;
