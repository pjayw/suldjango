import styled from 'styled-components/native';

const PopularRecoScreen = () => {
 return (
 <PopularRecoScreenBlock>
 
 </PopularRecoScreenBlock>
 );
};

const PopularRecoScreenBlock = styled.View``;

export default PopularRecoScreen;
